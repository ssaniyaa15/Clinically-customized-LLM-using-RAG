apiVersion: apps/v1
kind: Deployment
metadata:
  name: amca-redis
  namespace: amca
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: amca-redis
  template:
    metadata:
      labels:
        app.kubernetes.io/name: amca-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: amca-redis
  namespace: amca
spec:
  selector:
    app.kubernetes.io/name: amca-redis
  ports:
    - port: 6379
      targetPort: 6379

